import { FC } from 'react';
import MainList from '../features/MainList';

const HomePage: FC = ({}) => {
    return (
        <>
            <MainList />
        </>
    );
};

export default HomePage;
